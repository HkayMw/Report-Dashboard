# typical gitignore file for a Python project

venv/
__pycache__/
*.pyc



# Runtime phase state — created/edited by the live app (phases.py); never
# commit it, or a push would overwrite the hosted instance's live phases,
# assignments, and sheet URLs.
phases.json
masters/
